# ---------------------------------------------------------------------------
# icsmx-autob-webapp — variables de entorno
#
# Copiar a .env.local y completar. NUNCA versionar .env.local.
# En despliegue, estas variables se inyectan como secretos de Amplify.
# ---------------------------------------------------------------------------

# --- Identidad: Okta via @auth0/nextjs-auth0 -------------------------------
# Dominio del tenant de Okta, sin protocolo.
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=

# Secreto de cifrado de la cookie de sesion.
# Generar con:  openssl rand -hex 32
AUTH_SECRET=

# URL base de la aplicacion. Debe coincidir con la URL de callback
# registrada en Okta.
APP_BASE_URL=http://localhost:3000

# --- EAS: resolucion de permisos --------------------------------------------
# EAS no expone roles: se le envian nombres de permiso y responde un booleano
# por cada uno. Si no responde, la aplicacion falla de forma explicita; no hay
# fallback silencioso — usar ENABLE_DEV_TOOLS=MOCK_USERS para trabajar sin EAS.
#
# El contrato exacto (ruta y sobre HTTP) aun no esta confirmado: riesgo R19.
EAS_PROFILE_URL=
EAS_API_KEY=

# --- AWS --------------------------------------------------------------------
AWS_REGION=us-east-1

# Tabla unica de DynamoDB. La crea `npx ampx sandbox`.
AUTOB_TABLE_NAME=

# Bucket privado de fotografias y comprobantes.
AUTOB_MEDIA_BUCKET=

# --- CloudFront: fotografias con URL firmada -------------------------------
# Las URLs se firman en SSR en cada peticion.
# Nunca se persisten ni se generan dentro de un bloque "use cache".
#
# Los dos primeros los publica `npx ampx sandbox` en amplify_outputs.json,
# bajo custom.autob:
#   CLOUDFRONT_DOMAIN      <- distribucion
#   CLOUDFRONT_KEY_PAIR_ID <- llavePublicaCloudFront
#
# Ojo: es la llave **publica**, no el grupo de llaves. Poner
# grupoDeLlavesCloudFront produce un 403 de CloudFront que no dice cual de los
# dos identificadores esta mal.
#
# Y hay que **volver a copiarlos despues de cada `ampx sandbox delete`**: el
# redespliegue crea una distribucion nueva, con otro dominio. Firmar contra la
# anterior no da error de firma ni 403 — da una peticion que no llega a ningun
# lado, o sea una galeria vacia con la consola limpia. En desarrollo lo detecta
# `configuracionDeFirma`, que compara estas dos variables contra
# amplify_outputs.json y falla nombrando las dos. Ver desafios 50.
CLOUDFRONT_DOMAIN=
CLOUDFRONT_KEY_PAIR_ID=

# Llave privada en PEM, con los saltos de linea escapados como \n.
CLOUDFRONT_PRIVATE_KEY=

# --- CES: correo (Church Email Service) -----------------------------------
# El correo transaccional NO sale por SES. CES es un servicio REST externo:
# se le hace POST con un JSON y autenticacion basica. No es infraestructura de
# AWS, asi que no aparece en amplify/ — solo son credenciales en tiempo de
# ejecucion, que consume el procesador del outbox.
#
# AQUI SOLO SIRVEN EN LOCAL. En un backend desplegado, estas cuatro NO son
# variables de la consola de Amplify: son SECRETOS que consume el Lambda del
# barrido por `secret()` en amplify/backend.ts, y viven en SSM Parameter Store
# como /amplify/<parte-del-backend>/<NOMBRE>. En un sandbox se ponen con
#   npx ampx sandbox secret set CES_URL
# y en una rama de Amplify Hosting, desde la consola o escribiendo el
# SecureString en SSM — no hay comando de CLI para ramas. Ver runbooks.md R-14,
# seccion "CES — destino B".
#
# CES aun no esta aprobado para este proyecto (riesgo R17), y mientras siga asi
# NO hay que ponerlas en un ambiente de pruebas: con APP_ENV=pruebas el
# procesador del outbox (src/lib/correo/procesarOutbox.ts) marca el mensaje
# CANCELADO y deja en el registro la notificacion que habria enviado, que es con
# lo que se verifica el flujo sin bandeja de correo. En produccion, en cambio,
# falta de configuracion falla explicito (regla 15). La fila y la adjudicacion no
# se ven afectadas en ningun caso (D-6).
CES_URL=
CES_USER=
CES_PASSWORD=

# Direccion remitente que viaja en el cuerpo del mensaje.
CES_FROM_ADDRESS=

# --- Alarmas operativas (Etapa 12) -----------------------------------------
# Destinatario de los avisos de CloudWatch: barrido caido, correos sin
# entregar, vencimientos sin resolver, contencion de transacciones
# (amplify/alarmas.ts).
#
# Solo la lee `ampx` al desplegar, no la aplicacion en tiempo de ejecucion.
# Vacia, las seis alarmas se crean y cambian de estado igual —se ven en la
# consola de CloudWatch— pero no avisan a nadie: el tema de SNS queda sin
# suscriptores. Es lo deseable en un sandbox personal y lo que hay que llenar
# en un entorno vigilado.
#
# AWS envia un correo de confirmacion a esta direccion; la suscripcion no
# entrega nada hasta que se acepta (runbooks.md R-13).
ALARMAS_CORREO=

# --- Herramientas de desarrollo --------------------------------------------
# OFF                 solo servicios reales. UNICO valor admisible en produccion
# MOCK_USERS          permisos simulados por variable de entorno, sin EAS.
#                     Cambiar de actor exige editar este archivo y reiniciar
# FULL                lo anterior mas el conmutador de identidad simulada: una
#                     barra abajo a la derecha para elegir con quien se recorre
#                     la aplicacion, sin reiniciar y por navegador
#
# FULL es lo que hace probable el flujo completo, y no por comodidad: hay
# guardas que dependen de la IDENTIDAD y no de los permisos. `convocatoria:aprobar`
# deniega `self_approval`, asi que el dictamen exige dos identidades distintas;
# y la fila FIFO no tiene orden con un solo participante. Ninguna de las dos se
# alcanza simulando permisos. El roster esta cerrado en
# src/lib/auth/personasSimuladas.ts y la cookie solo lleva el id de la persona.
#
# La impersonacion NO sustituye la autenticacion: sigue exigiendo una sesion
# real de Okta, y `oktaSub` conserva el valor real.
#
# En un DESPLIEGUE compilado, cualquier valor distinto de OFF exige ademas
# APP_ENV=pruebas (abajo). Sin ella, la aplicacion lanza.
ENABLE_DEV_TOOLS=FULL

# --- Entorno del despliegue -------------------------------------------------
# produccion | pruebas.  AUSENTE O DESCONOCIDO SE ASUME produccion.
#
# Solo hace falta en un DESPLIEGUE. En local no se pone: ahi basta con que
# NODE_ENV no sea production.
#
# Existe porque Amplify Hosting compila y sirve TODA rama con
# NODE_ENV=production, incluida una de pruebas, asi que NODE_ENV no distingue
# produccion de un ambiente de prueba — y sin esa distincion no se puede
# desplegar un ambiente de prueba con el conmutador de identidades, que es
# justo donde hace falta (el flujo completo exige dos identidades distintas).
#
# Que la ausencia se asuma produccion es la propiedad importante: olvidar la
# variable deja las herramientas BLOQUEADAS, no abiertas (regla 18).
#
# LO QUE ABRE con APP_ENV=pruebas, y hay que decidirlo a sabiendas: con
# cualquier modo distinto de OFF, `src/lib/auth/eas.ts` NO CONSULTA EAS. Quien
# se autentique recibe los permisos de DEV_TOOLS_MOCK_ROLES —ADMINISTRADOR si
# no se pone— y, en FULL, puede cambiarse a cualquier identidad del roster. La
# autenticacion sigue siendo real y obligatoria, pero deja de implicar
# autorizacion: cualquier cuenta del tenant de Okta que alcance la URL es
# administradora. Aceptable con datos desechables; nunca con datos reales.
# APP_ENV=pruebas

# Roles simulados cuando ENABLE_DEV_TOOLS no es OFF. Lista separada por comas.
# src/lib/auth/rolesSimulados.ts los traduce a permisos con la tabla de la
# seccion 8 de agent_files/permission-matrix.md — es la traduccion que en
# produccion hace EAS. Sin esta variable, se simula un solo ADMINISTRADOR.
#
# En FULL es el comportamiento de respaldo: solo aplica cuando no hay ninguna
# persona elegida en la barra.
DEV_TOOLS_MOCK_ROLES=ADMINISTRADOR

# Permisos sueltos, para combinaciones que ningun rol representa (por ejemplo
# tesoreria sin auditoria). Tiene PRECEDENCIA sobre DEV_TOOLS_MOCK_ROLES.
# Nombres validos en src/types/identidad.ts (PERMISOS).
# DEV_TOOLS_MOCK_PERMISOS=Autob_Operar_Tesoreria,Autob_Auditar

# ---------------------------------------------------------------------------
# Fuera de este archivo, en variables de entorno del sistema:
#
#   NODE_AUTH_TOKEN       token del Artifactory privado (.npmrc).
#                         Sin el, `npm install` falla con 401.
#
#   NODE_EXTRA_CA_CERTS   ruta al PEM del CA corporativo, en maquinas con
#                         inspeccion TLS. Sin el, npm, ampx y los SDK de AWS
#                         fallan con UNABLE_TO_VERIFY_LEAF_SIGNATURE.
#                         Ver agent_files/desafios-implementacion.md, seccion 1.
# ---------------------------------------------------------------------------
